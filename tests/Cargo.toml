[package]
name = "aurorac_testing"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
aurorac_macros = { version = "0.1.0", path = "../macros" }
